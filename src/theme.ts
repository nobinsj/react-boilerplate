import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2"
    },
    secondary: {
      main: "#ff4081",
    },
    background: {
      default: "#f4f4f4"
    },
    text: {
      primary: "#333"
    }
  }
})

export default theme